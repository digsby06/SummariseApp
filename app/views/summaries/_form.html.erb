<%= simple_form_for(@summary) do |f| %>
   <% if @summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@summary.errors.count, "error") %> prohibited this summary from being saved:</h2>

      <ul>
      <% @summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <%= f.input :post_title, :input_html => {:style => 'width: 200px'} %>
    <%= f.input :summary_entry, :input_html => {:style => 'width: 200px'} %>
  </div>


  <div class="field">
    <%= f.label :post_title %><br>
    <%= f.text_field :post_title %>
  </div>
  <div class="field">
    <%= f.label :summary_entry %><br>
    <%= f.text_area :summary_entry %>
  </div>
  <div class="hidden">
    <%= f.number_field :user_id, value: current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

   <div class="hidden">
    <%= f.number_field :user_id, value: current_user.id %>
  </div>
<% end %>
